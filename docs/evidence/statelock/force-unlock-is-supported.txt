$ terraform force-unlock -force 56544de9-10e5-6d82-6192-d8f5050ec8e7
# The lock id was read out of a refusal while a real apply held it. That apply was then
# SIGKILLed, so by the time force-unlock ran there was nothing left to unlock.
# exit code: 0
# granted advisory locks immediately before force-unlock ran: 0

Terraform state has been successfully unlocked!

The state has been unlocked, and Terraform commands should now be able to
obtain a new lock on the remote state.

The specification said force-unlock is unsupported on the pg backend. It is supported.
What is true, and is the more useful half, is that it is never needed: the lock cannot
go stale, so there is never a row to clear.
